#!/bin/sh
# Generated by phorge-prepare. Runtime changes require container recreation.
set -eu

VCSUSER='{{ vcs_user }}'
ROOT='{{ phorge_root }}'

if [ "$#" -ne 1 ] || [ "$1" != "$VCSUSER" ]; then
  exit 1
fi

exec "$ROOT/bin/ssh-auth" "$@"
